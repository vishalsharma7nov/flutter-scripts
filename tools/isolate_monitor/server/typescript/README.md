# TypeScript backend (scaffold)

Implement the isolate monitor server here (Node.js):

- HTTP `/api/*` matching the Dart server contract
- Static UI from `../../web`
- Workers for logs / deploy / VM / screen mirror

## Status

Not runnable yet. Preference can still be set to `typescript` so work continues in this directory.

## Next steps

1. `npm install`
2. Implement `src/index.ts`
3. Wire `open-isolate-monitor.sh --backend typescript` to `npm start`

## Stub

```bash
npm start
```

Currently exits with a clear “not implemented” message.
