name: isolate_monitor
description: Local web GUI for inspecting Dart isolates via the VM service.
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  args: ^2.7.0
  http: ^1.2.2
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  shelf_static: ^1.1.3
  vm_service: ^15.0.0
