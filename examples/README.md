# Examples

Copy these into place and edit — they are **not** auto-discovered for releases
until you place a config under `packages/`.

| Path | Use |
|------|-----|
| `flutter_git_package/release.config.sh` | Sample → copy to `packages/<id>/release.config.sh` |

See [docs/PACKAGE_RELEASE.md](../docs/PACKAGE_RELEASE.md).
